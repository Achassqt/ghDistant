# Hook pre-commit

Ce hook pre-commit est configuré pour exécuter une action avant chaque commit dans ce projet Git

## Installation

copier le fichier pre-commit du dossier hooks dans le répertoire des hooks Git

cp hooks/pre-commit .git/hooks/pre-commit